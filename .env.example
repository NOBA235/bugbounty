# Google Gemini API key (server-side only — never expose to client)
GEMINI_API_KEY=

# Optional Supabase (falls back to localStorage if unset)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
